/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["scontent-qro1-1.xx.fbcdn.net", "res.cloudinary.com"], // Agrega aquí el dominio de las imágenes
      },
};

export default nextConfig;